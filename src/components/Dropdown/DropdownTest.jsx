import Dropdown, {DropdownButton, DropdownItem} from "./Dropdown";
import { render, screen, fireEvent } from '@testing-library/react';
import user from '@testing-library/user-event'

describe(Dropdown, () => {
    it("Dropdown displays as expected.", () => {
       // const {} = render
    })
})